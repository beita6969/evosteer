# 七项 IID 32-sample：修复后真实评测

**运行已完成，但不能宣称所有问题／分数目标均已解决。**
运行 `seven-iid32-a2-owner-2ae5b87-20260908T063713Z`，源码 `2ae5b87`。
2026-09-08 06:37:15–07:29:29 UTC，端到端 **52.24 分钟**。
[完整聚合数据](machine-results/step0_seven_iid32_native_repair_2026-09-08.json)。

后续单项更新：HealthBench 在 `0937752` 的独立同32题修复轮取得 **45.03364**，
见[对话/history修复报告](step0_healthbench_context_diagnosis_2026-09-08.md)。
下表仍保留原七项同轮成绩，不能把后续单项结果混称为新完成的一整轮七项评测。

固定来源顺序每项取32题；AIME只有30题，全部使用、不复制，因此共 **222/222**。
只评A2；Qwen3.5-9B，零训练、无adapter、skills off、seed0，实际严格单owner、peer调用0。
AIME thinking-on，其余六项thinking-off。WebShop不运行。主Agent协作拓扑没有调整。
数据已用于开发；没有匹配backbone对照，不能据此宣称架构因果增益。

| Benchmark | 数量 | 主分数（0–100） | 说明 |
|---|---:|---:|---|
| HotpotQA | 32 | **75.01** | Answer F1；EM56.25；不是新的128题成绩 |
| TriviaQA | 32 | **81.98** | Answer F1；EM78.125 |
| AIME2026 | 30 | **53.33** | 16正确、8提交后错误、6未提交；不合并旧轮25/30 |
| HealthBench | 32 | **38.45** | 本地Qwen rubric均分，不是官方外部judge成绩 |
| ALFWorld | 32 | **84.375** | 27成功；22 seen / 10 unseen，分别19 / 8成功 |
| MBPP+ | 32 | **78.125** | Base AND Plus：25；Base27、独立Plus25；hard子集未验证 |
| HumanEval | 32 | **87.50** | 28通过；2测试失败、2运行错误 |

## 已完成与仍未解决

- Python载体统一解码：裸源码及常见围栏别名；多份实现由owner整合，不隐藏测试择优。
- MBPP固定原生4秒/4倍时限与完整scorer profile；独立执行Base/Plus，保留原生诊断。
  标准公开prompt逐字段来源比对保持接口/示例，私有测试与答案不进入actor。
- AIME支持明确最终字段后附解释；原始token流续写、reasoning状态保存、有效配置导出。
  实际58次调用、23次续写，全部thinking-on；没有从未闭合推理提取答案。
- **AIME仍未通过输出验收。** 六条失败各用尽81,920输出token。最后4,096 token仍用于
  同一thinking流，并非保证进入独立final提交阶段。分块与预留没有证明解决长尾提交问题。
  16/30明显低于历史25/30；不能把预算修复等同于准确率改善，原因尚未完成受控归因。
  后续需要继续处理最终提交预算和回退原因，不恢复咨询Agent、不按题挑答案或追加计算。
- [HotpotQA专项检查](step0_hotpot_context_diagnosis_2026-09-08.md)：本轮每题10段上下文
  完整进入实际请求和tokenizer，官方评分逐项一致。另修复owner删除答案字段边界后
  把后续解释并入答案的问题。该修复在 `b40257b`，不是本轮重新生成的源码版本。
  对全部32 HotpotQA + 32 TriviaQA已保存输出进行只读投影比较，提交值变化0；不追改分数。
- 一次MBPP原生worker因子进程退出后的共享锁停滞，无原生verdict；只重试同一已保存
  候选、同一scorer条件后完成。随后32条统一raw-IPC复评，与原verdict全部一致。
  [128条旧候选时限比较](machine-results/step0_mbpp_saved_candidate_time_comparison_2026-09-08.json)
  也没有改变主分数：两条件均93/128。诊断未反馈actor，原分数未覆盖。

## 成本、验证与进程

- Actor：1,482次模型调用，11,580,220输入token、1,075,244输出token。
  HealthBench盲评分：400次请求/响应，533,796输入token、41,827输出token。
  已完成评分的wall-time合计不是端到端时间，也不包含未单独计时的CPU锁停滞；恢复事件
  在聚合数据单列，不能把最终缺失分数为0误读成运行期间从未发生基础设施故障。
- 先只读评分已提交候选，再与AIME长尾重叠完成HealthBench评分；主流程复用的是
  **本轮已经生成的216个评分**，不是历史答案或跨策略分数。其余6个最终评分由主流程完成。
- 局部验证：初始修复135项，后续受影响9项与配置导出18项；IPC相关37项；
  Hotpot输入5项，QA/AIME/普通消息相关78项；修改文件Ruff和两文件mypy通过。
- 初始集成 `80149dd`：22049 CPU完整检查3,425通过、1 GPU-only跳过。
  最终发布 `b40257b`：22049 CPU `CUDA_VISIBLE_DEVICES="" make check`，
  **3,443通过、1 GPU-only跳过**，Ruff、527源码mypy、两个wheel和模型包边界检查通过。
  最终使用干净提交快照；复用目录中的旧缓存/pytest生成文件曾使format阶段误扫描，
  已将缓存、临时目录移至源码外。没有把那两次环境问题称为源码测试失败。
- 文档更新不再重复全套验证；QA提交值不变不追加64次模型生成；未运行无关前端E2E。
  没有独立审查/编码子代理，仅使用既有watching代理；没有哈希校验。
- 只使用22048现有GPU5/7。评测及辅助评分进程已结束；按owner要求，两台SGLang服务
  保持在线驻留。未启动训练、未使用额外GPU、未修改其他会话进程。

所有题面、答案、逐题结果、私有路径和连接凭据留在私有存储。
