# Historical Protocol-v3 artifacts

The two JSON files in this directory are retained only as historical `@3`
examples. Active readers require the result-blind protocol and freeze wire
format `@8`; these files are not executable inputs and are not a private frozen
benchmark schedule.
